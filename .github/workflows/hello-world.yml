name: Hello World Container

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  hello-world:
    runs-on: windows-2025-vs2026
    container:
      image: ubuntu:latest
    steps:
      - name: Print hello world
        shell: sh
        run: echo "hello world"
